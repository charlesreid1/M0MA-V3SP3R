from __future__ import annotations

from mcp.server.fastmcp import FastMCP


def register_resources(mcp: FastMCP) -> None:
    """Stub — real handlers land in feature/mcp-tools-knowledge (§10.3)."""
    return None
